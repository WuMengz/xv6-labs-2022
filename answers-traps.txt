1. a0~a7 八个寄存器。是第三个参数，因此是 a2。
2. 编译器优化掉了 f 对 g 的 call 和 main 对 f 的 call，也优化掉了 +1 的操作，直接将结果 12 存入了 a1。
3. 64a。跳转方法是 auipc 和 jalr 的配合。
4. 38。ra 寄存器是 return address
5. He110 World。0x726c6400。不需要。
6. a2 寄存器中原来的值，本次测试中为 1491381352。因为没有为 printf 的第三个参数也就是 a2 准备值，所以 printf 会直接使用 a2 原来的值输出。